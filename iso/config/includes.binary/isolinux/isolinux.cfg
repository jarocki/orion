# GENERATED — do not edit — regenerate via scripts/build-iso.sh
#
# @decision DEC-PHASE11-012
# @title Bootloader cmdline single-authority (issue #64)
# @status accepted
# @rationale This file is generated by scripts/build-iso.sh::generate_bootloader_configs()
#   from the single --bootappend-live source in iso/auto/config. Direct edits
#   will be overwritten on the next build. The rc7-rc9 dual-authority arc (where
#   this file was hand-edited without effect on /proc/cmdline) is retired.
#   The dual-authority hazard identified in DEC-PHASE10-018 is superseded by DEC-PHASE11-012
#   consolidates to a single --bootappend-live source.
#   References: DEC-PHASE11-012, DEC-PHASE10-018 (superseded), issue #64, issue #65.
#
# Generated from: iso/auto/config::--bootappend-live
# Active cmdline (live label):
#   boot=live components splash quiet persistence console=tty0 console=ttyS0,115200n8
#   live-config.username=orionx-operator live-config.hostname=orionx
#
# timeout: units are 1/10th second. timeout 10 = 1.0 second before auto-boot.
# prompt 0: do not prompt for user input (auto-boot after timeout).

serial 0 115200
default live
timeout 10
prompt 0

label live
    menu label Orion-X Live
    kernel /live/vmlinuz
    initrd /live/initrd.img
    append boot=live components splash quiet persistence console=tty0 console=ttyS0,115200n8 live-config.username=orionx-operator live-config.hostname=orionx

label live-failsafe
    menu label Orion-X Live (failsafe)
    kernel /live/vmlinuz
    initrd /live/initrd.img
    append boot=live components splash quiet persistence console=tty0 console=ttyS0,115200n8 live-config.username=orionx-operator live-config.hostname=orionx nosplash text noapic noapm nodma nomce nosmp nolapic
