# W11-6 Layer A lazy-start override — DEC-PHASE11-008
# Suricata does not auto-start; operator or Nebula tier selector enables via:
#   sudo touch /var/lib/suricata/orionx-enabled && sudo systemctl start suricata
[Unit]
ConditionPathExists=/var/lib/suricata/orionx-enabled
