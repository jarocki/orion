#include <tunables/global>

profile wireguard /usr/bin/wg {
  #include <abstractions/base>

  /etc/wireguard/** rw,
  /var/run/orionx-mesh* rw,

  capability net_admin,
  capability net_raw,
  network inet dgram,
  network inet6 dgram,

  deny /home/** rw,
}
