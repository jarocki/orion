# Orion-X Nebula MCP Servers

MCP (Model Context Protocol) servers register here for Nebula runtime consumption.

## Layer B — remnux-mcp-server (DEFERRED to W11-3b)

The remnux-mcp-server integration is deferred to W11-3b. When landed, it will:
- Be a stdio LOCAL MCP server (DEC-006 LOCAL preserved)
- Run under AppArmor (DEC-007)
- Provide RE-tool orchestration for Nebula chat

## Placeholder

This directory is intentionally empty in W11-3 Layer A. W11-3b will populate `remnux/` subdirectory with the server source, node_modules, and Node.js runtime.
